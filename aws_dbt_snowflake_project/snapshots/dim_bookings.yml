snapshots:
  - name: dim_bookings
    relation: ref('bookings')
    description: ephemeral model from gold layer
    config:
      database: AIRBNB
      schema: gold
      strategy: timestamp
      unique_key: BOOKING_ID
      updated_at: CREATED_AT
      dbt_valid_to_current: "to_date('9999-12-31')"
