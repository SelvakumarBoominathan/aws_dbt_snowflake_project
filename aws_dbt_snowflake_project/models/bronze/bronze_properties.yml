version: 2

models:
  - name: bronze_bookings
    config:
      materialized: view

  - name: bronze_hosts
    config:
      materialized: view

  - name: bronze_listings
    config:
      materialized: view
